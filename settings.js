module.exports = {
    // your login for woz.ch - won't be sent anywhere else than to woz.ch for fetching the paywall articles
    name: 'yourname@domain.ch',
    // your password for woz.ch - won't be sent anywhere else than to woz.ch for fetching the paywall articles
    pass: '*******',
    // the path to your eBook reader, so the script can directly copy the file onto your reader
    // (leave empty string for no copying of the book: '')
    readerPath: '/Volumes/tolino/Books/Added' // the path
};
